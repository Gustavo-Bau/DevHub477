{
  "name": "devhub477-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "postinstall": "prisma generate",
    "check:no-ts": "bash -lc \"if rg --files -g '*.ts' -g '*.tsx' -g '*.d.ts' app components contexts lib pages | grep -q .; then echo 'TypeScript files found'; exit 1; else echo 'No TypeScript files found'; fi\""
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@prisma/client": "^6.5.0",
    "bcryptjs": "^2.4.3",
    "next-auth": "^4.24.11",
    "react-hook-form": "^7.56.2",
    "stripe": "^18.2.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  },
  "engines": {
    "node": "20.x"
  }
}
