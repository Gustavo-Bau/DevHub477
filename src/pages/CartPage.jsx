import Seo from '../seo/Seo';import { useCart } from '../context/CartContext';import LoadingState from '../components/LoadingState';import EmptyState from '../components/EmptyState';
export default function CartPage(){const {items,loading,error,total}=useCart();if(loading)return <LoadingState message='Carregando carrinho...'/>;if(error)return <EmptyState title='Carrinho indisponível' message={error}/>;if(!items.length)return <EmptyState title='Carrinho vazio' message='Adicione um produto para continuar.'/>;return <section className='card'><Seo title='Carrinho | DEVHUB' description='Revise itens do carrinho.' path='/carrinho'/><h1>Carrinho</h1><ul>{items.map(i=><li key={i.id||i.slug}>{i.name} × {i.quantity}</li>)}</ul><p><strong>Total:</strong> {new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(total)}</p></section>}
